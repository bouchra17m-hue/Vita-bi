{
  "version": 2,
  "buildCommand": "npm --prefix pfevitabi run build",
  "outputDirectory": "pfevitabi/dist",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
