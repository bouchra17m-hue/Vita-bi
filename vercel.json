{
  "version": 2,
  "buildCommand": "cd pfevitabi && npm install && npm run build",
  "outputDirectory": "pfevitabi/dist",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
